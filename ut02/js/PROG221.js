function isNumber( str ){
    let resultado;
    if(isNumber(str)){
        resultado = true;
    }else{
        resultado = false;
    }
    alert( resultado );
}
let n = prompt ("escriba lo que sea: ");
alert( isNumber( n ) );